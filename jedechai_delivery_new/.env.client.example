# ไฟล์นี้ถูก bundle เข้าแอป (Flutter asset) — ใส่ได้เฉพาะค่าที่เปิดเผยได้เท่านั้น
# ห้ามใส่ service key / secret key / private key / database URL เด็ดขาด
# secret ฝั่ง server ให้อยู่ใน .env (ไม่ bundle) หรือ `supabase secrets set`
SUPABASE_URL=
SUPABASE_ANON_KEY=
GOOGLE_MAPS_API_KEY=
PASSWORD_RESET_REDIRECT_URL=
FIREBASE_PROJECT_ID=
