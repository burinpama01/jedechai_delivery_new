// Basic smoke test for JeDeChai Delivery app
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App package can be referenced', () {
    // Verify the test framework is working
    expect(1 + 1, equals(2));
  });
}
